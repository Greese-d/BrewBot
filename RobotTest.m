hold on;  % Hold the current plot for multiple objects
axis equal;  % Maintain equal scaling for all axes
axis([-0.7 0.7 -0.7 0.7 -0.7 0.7]);  % Set the axis limits
    
% a = PlaceObject('DobotNova2Link1.ply', [0, 0, 0]);  % Load shelf model and position it
% b = PlaceObject('DobotNova2Link1.ply', [0.2, 0, 0]);  % Load shelf model and position it
% c = PlaceObject('DobotNova2Link2.ply', [0, 0.2, 0]);  % Load shelf model and position it
% d = PlaceObject('DobotNova2Link3.ply', [-0.2, 0, 0]);  % Load shelf model and position it
% e = PlaceObject('DobotNova2Link4.ply', [0, -0.2, 0]);  % Load shelf model and position it
% f = PlaceObject('DobotNova2Link5.ply', [0, 0, 0.2]);  % Load shelf model and position it
r = DobotNova2
pause()
